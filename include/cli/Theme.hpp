#pragma once

#include <string>

namespace Theme
{
    std::string reset();

    std::string primary();

    std::string success();

    std::string warning();

    std::string error();

    std::string accent();
}